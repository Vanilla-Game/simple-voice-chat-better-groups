name: Release Please

on:
  push:
    branches: [ main ]

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Create or update release PR
        uses: googleapis/release-please-action@v5
        id: release
        with:
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json

      # A tag created by the repository GITHUB_TOKEN does not trigger build.yml.
      # Build the released revision here and attach the plugin jar directly.
      - name: Checkout
        if: ${{ steps.release.outputs.release_created }}
        uses: actions/checkout@v7

      - name: Set up JDK 25
        if: ${{ steps.release.outputs.release_created }}
        uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: temurin

      - name: Set up Gradle
        if: ${{ steps.release.outputs.release_created }}
        uses: gradle/actions/setup-gradle@v5

      - name: Build server and client jars
        if: ${{ steps.release.outputs.release_created }}
        run: ./gradlew --no-daemon clean build --warning-mode all

      - name: Attach server and client jars to release
        if: ${{ steps.release.outputs.release_created }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload "${{ steps.release.outputs.tag_name }}" build/libs/*.jar client-fabric/build/libs/*.jar --clobber
