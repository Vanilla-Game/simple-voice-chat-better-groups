# How long a one-time invite remains valid.
invites:
  expiration-minutes: 5

  # Delay between successfully validated invite attempts, applied per
  # inviter and target pair. Set to 0 to disable the cooldown.
  cooldown-seconds: 10

# Join requests for password-protected groups.
requests:
  expiration-minutes: 5

  # Delay between join requests, applied per requester and group pair.
  # Set to 0 to disable the cooldown.
  cooldown-seconds: 30

  # Sound played to the group leader when a join request arrives. Any sound
  # event id works (e.g. entity.experience_orb.pickup); set to none to disable.
  sound: block.anvil.land

  # Volume (0.0-10.0) and pitch (0.5-2.0, the range Minecraft supports) of
  # that sound.
  sound-volume: 1.0
  sound-pitch: 1.0
